diff -Nur rasqal-0.9.29-clean/configure rasqal-0.9.29/configure
--- rasqal-0.9.29-clean/configure	2012-07-31 23:06:34.000000000 -0400
+++ rasqal-0.9.29/configure	2012-07-31 23:07:40.000000000 -0400
@@ -15271,9 +15271,9 @@
 
   LIBS="$oLIBS"
 fi
-if test $ceil_lib = none; then
-  as_fn_error $? "Could not find ceil, floor, round in default libs or with -lm" "$LINENO" 5
-fi
+#if test $ceil_lib = none; then
+#  as_fn_error $? "Could not find ceil, floor, round in default libs or with -lm" "$LINENO" 5
+#fi
 if test $need_libm = 1; then
   RASQAL_EXTERNAL_LIBS="$RASQAL_EXTERNAL_LIBS -lm"
 fi
